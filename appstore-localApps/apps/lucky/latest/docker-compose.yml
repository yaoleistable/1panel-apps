services:
  lucky:
    container_name: ${CONTAINER_NAME}
    restart: always
    network_mode: host
    volumes:
      - ${DATA_PATH}:/goodluck
    image: gdy666/lucky:latest
    labels:  
      createdBy: "Apps"
